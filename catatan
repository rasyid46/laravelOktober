 if(Sentinel::getUser()->roles()->first()->slug == 'administrator'){
               return $next($request);

          }else{
                return   redirect()->route('dashboard');
          }



if ($old_image) {
        $pathOldImage = public_path($pathImage . $old_image);
        if (file_exists($pathOldImage)) {
            !unlink(public_path() . $pathImage . $old_image);
        }
}